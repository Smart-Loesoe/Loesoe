services:
  web2:
    container_name: loesoe-web-5174
    build:
      context: ..
      dockerfile: infra/Dockerfile
      args:
        VITE_API_BASE: "http://localhost:8000"
        WEB_DIR: "web"
    ports:
      - "5174:3000"
    depends_on:
      api:
        condition: service_healthy
